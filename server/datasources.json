{
  "db": {
    "host": "${DB_HOST}",
    "port": "5432",
    "database": "app",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "name": "db",
    "debug": true,
    "connector": "postgresql"
  }
}