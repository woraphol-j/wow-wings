{
  "db": {
    "host": "${DB_HOST}",
    "port": "54321",
    "database": "app",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "name": "app",
    "debug": true,
    "connector": "postgresql"
  }
}